export type RoomProperties = {
    isLocked: boolean;
    name:string;
    lang: string;
    totalPlayers: number;
    maxPlayers: number;
}

export type PlayerScores = {
    name: string;
    score: number;
}
